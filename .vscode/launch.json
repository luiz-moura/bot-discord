{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "hostname": "0.0.0.0",
            "log": true,
            "port": 9003,
            "pathMappings": {
                "/app" : "${workspaceFolder}"
            },
        }
    ]
}
